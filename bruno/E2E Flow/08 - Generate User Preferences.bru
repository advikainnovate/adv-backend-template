meta {
  name: 08 - Generate User Preferences
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/user-preferences/generate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # 08 - Generate User Preferences
  
  Triggers generation of the user's notification preferences to ensure no channels are blocked during dispatch checks.
}
