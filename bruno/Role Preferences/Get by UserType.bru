meta {
  name: Get by UserType
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/role-preferences/role/customer
  body: none
  auth: none
}

docs {
  # Get Role Preferences by UserType
  
  Retrieves all role default notification preferences configured for a specific role / user type.
}
