meta {
  name: Delete
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/role-preferences/replace-with-id
  body: none
  auth: none
}

docs {
  # Delete Role Preference
  
  Deletes (soft deletes) a role default preference record.
}
